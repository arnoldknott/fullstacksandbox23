# Infrastructure

The [infrastructure](../../infrastructure/) directory contains OpenTofu configuration for Azure resources and the `dev`, `stage`, and `prod` workspace model. Infrastructure commands run through the existing container and repository scripts; deployment details are maintained in [infrastructure/README.md](../../infrastructure/README.md) and [infrastructure/AGENTS.md](../../infrastructure/AGENTS.md).
