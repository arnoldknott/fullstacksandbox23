# Internal data transfer

Internal transfer uses REST for initial entity snapshots and Socket.IO for authorized subscriptions, cursor replay, and incremental mutations. See [REST snapshots and incremental Socket.IO](rest-snapshot-incremental-socketio.md) for the current contract.
