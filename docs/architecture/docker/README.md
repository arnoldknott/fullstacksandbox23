# Docker

Docker Compose coordinates development, test, production, and infrastructure services from the repository root. [compose.yml](../../../compose.yml) defines the shared stack, the override files select environment behavior, and [.devcontainer/devcontainer.json](../../../.devcontainer/devcontainer.json) provides the primary VS Code workspace container.
