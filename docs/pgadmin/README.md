# pgAdmin

pgAdmin is an optional development interface for inspecting and administering PostgreSQL. Its repository-specific helper for setting the master password lives in [pgadmin](../../pgadmin/), and the service is configured by the root development Docker Compose stack.
