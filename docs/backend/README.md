# Backend

The [backend](../../backend/) is a FastAPI application that exposes REST, Socket.IO, and WebSocket interfaces. SQLModel models, asynchronous CRUD classes, access policies, access logs, Celery jobs, and Alembic migrations live under `backend/src`; repository-specific development and validation guidance is maintained in [backend/AGENTS.md](../../backend/AGENTS.md).
