import { describe, it, expect } from 'vitest';

describe('calc tests', () => {
	it('adds 1 + 2 to equal 3', () => {
		expect(1 + 2).toBe(3);
	});
	it('subtracts 15 -10 to equal 5', () => {
		expect(15 - 10).toBe(5);
	});
});
